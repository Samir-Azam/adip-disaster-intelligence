# Local Development

Prerequisites: Git, Node.js LTS, Python 3.x, Docker Desktop.

Required environment examples:
backend/.env.example
ai-service/.env.example
frontend/.env.example

Target first-run:
clone → env setup → docker compose → migrations → seed demo data → open UI → tests.
